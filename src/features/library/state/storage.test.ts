import{describe,expect,it}from"vitest";import{defaultReadingState,parseReadingState,readStorage,writeStorage}from"./storage";
describe("reading storage",()=>{it("falls back for malformed and old data",()=>{expect(parseReadingState("nope")).toEqual(defaultReadingState());expect(parseReadingState('{"version":0}')).toEqual(defaultReadingState())});it("sanitizes fields and recent order",()=>{const state=parseReadingState(JSON.stringify({version:1,bookmarks:["a","a",3],recent:[{id:"a",viewedAt:1},{id:"b",viewedAt:3},{bad:true}],lastTopic:{a:"topic",b:2},completion:{a:true,b:"yes"}}));expect(state.bookmarks).toEqual([]);expect(state.recent.map(x=>x.id)).toEqual(["b","a"]);expect(state.lastTopic).toEqual({a:"topic"});expect(state.completion).toEqual({a:true})});it("persists bookmarks, recent, and completion",()=>{let raw:string|null=null;const storage={getItem:()=>raw,setItem:(_:string,value:string)=>{raw=value}};const state={...defaultReadingState(),bookmarks:["a"],recent:[{id:"a",viewedAt:5}],completion:{a:true}};writeStorage(state,storage);expect(readStorage(storage)).toEqual(state)});it("survives blocked storage",()=>expect(readStorage({getItem(){throw new Error("blocked")}})).toEqual(defaultReadingState()))});
