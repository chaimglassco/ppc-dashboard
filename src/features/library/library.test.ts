import{describe,expect,it}from"vitest";import{extractTopics}from"./domain/headings";import{filterDocuments}from"./domain/search";import{getDocumentBySlug,getPublishedDocuments,loadAllDocuments,validateDocuments}from"./data/repository";
describe("library repository",()=>{it("loads all eight starter documents",()=>expect(loadAllDocuments()).toHaveLength(8));it("returns only published visible documents",()=>expect(getPublishedDocuments().every(d=>d.status==="published"&&!d.hidden)).toBe(true));it("rejects duplicate IDs and slugs",()=>{const doc=getPublishedDocuments()[0];expect(()=>validateDocuments([doc,{...doc,slug:"other"}])).toThrow(/Duplicate document ID/);expect(()=>validateDocuments([doc,{...doc,id:"other"}])).toThrow(/Duplicate document slug/)});it("resolves valid slugs and rejects invalid slugs",()=>{expect(getDocumentBySlug("amazon-ppc-campaign-structure-sop")?.id).toBe("lib-campaign-structure");expect(getDocumentBySlug("not-a-document")).toBeUndefined()})});
describe("search and filters",()=>{const docs=getPublishedDocuments();it.each([["organic","lib-organic-ranking"],["repeatable naming","lib-campaign-structure"],["workflow","lib-weekly-checklist"],["template","lib-client-report"]])("matches %s case-insensitively",(q,id)=>expect(filterDocuments(docs,{q:q.toUpperCase()}).some(d=>d.id===id)).toBe(true));it("combines search, category, and type",()=>expect(filterDocuments(docs,{q:"negative",category:"Search Term Optimization",type:"Playbook"}).map(d=>d.id)).toEqual(["lib-negative-framework"]));it("clearing filters restores catalog",()=>expect(filterDocuments(docs,{})).toHaveLength(docs.length))});
describe("headings",()=>{it("creates deterministic unique IDs",()=>expect(extractTopics("## Review\n### Step\n## Review").map(t=>t.id)).toEqual(["review","step","review-2"]))});
