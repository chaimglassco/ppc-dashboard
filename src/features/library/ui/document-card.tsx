"use client";
import { AlertTriangle, ChevronDown, ChevronUp, Eye, EyeOff, FileText, Trash2 } from "lucide-react";
import Link from "next/link";
import type { LibraryDocument } from "../domain/types";
import { BookmarkButton } from "./bookmark-button";

export type DocumentAdminActions = { onToggleHidden: () => void; onDelete: () => void; onMoveUp: () => void; onMoveDown: () => void; canMoveUp: boolean; canMoveDown: boolean };
export function DocumentCard({ doc, admin, needsRecovery = false }: { doc: LibraryDocument; admin?: DocumentAdminActions; needsRecovery?: boolean }) {
  return <article className={`document-card${doc.hidden ? " hidden-document" : ""}${needsRecovery ? " document-card--needs-recovery" : ""}`}><div className="document-copy"><div className="card-top"><span className="type-badge">{doc.type}</span>{needsRecovery ? <span className="recovery-status-badge"><AlertTriangle aria-hidden="true" /> Needs recovery</span> : <span className="section-count"><FileText aria-hidden="true" /> {doc.topics.filter(topic => topic.level === 2).length} {doc.topics.filter(topic => topic.level === 2).length === 1 ? "section" : "sections"}</span>}{admin && <span className={doc.hidden ? "visibility-badge hidden" : "visibility-badge"}>{doc.hidden ? <EyeOff /> : <Eye />}{doc.hidden ? "Hidden" : "Visible"}</span>}</div><h2>{doc.title}</h2><p>{needsRecovery ? "This document’s active record is incomplete. Open its protected preview to review or restore the newest valid version." : doc.description}</p>{admin && !needsRecovery && <div className="document-admin-actions"><button type="button" onClick={admin.onToggleHidden}>{doc.hidden ? <Eye /> : <EyeOff />}{doc.hidden ? "Show" : "Hide"}</button><button type="button" onClick={admin.onMoveUp} disabled={!admin.canMoveUp} aria-label={`Move ${doc.title} up`}><ChevronUp /> Up</button><button type="button" onClick={admin.onMoveDown} disabled={!admin.canMoveDown} aria-label={`Move ${doc.title} down`}><ChevronDown /> Down</button><button className="danger" type="button" onClick={admin.onDelete}><Trash2 /> Delete</button></div>}</div><div className="card-actions">{!needsRecovery ? <BookmarkButton id={doc.id} compact /> : null}{!doc.hidden && <Link className="read-button" prefetch={false} href={`/library/${doc.slug}`}>{needsRecovery ? "Preview recovery version" : "Read Document"}</Link>}</div></article>;
}
