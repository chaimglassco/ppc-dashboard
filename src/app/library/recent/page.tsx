import Link from "next/link";import{getPublishedDocuments}from"@/features/library/data/repository";import{SavedList}from"@/features/library/ui/saved-list";export const metadata={title:"Recent"};export default function Page(){return<div className="page"><header className="page-header"><div><span className="eyebrow">PICK UP WHERE YOU LEFT OFF</span><h1>Recently viewed</h1><p>Your reading history, newest first, stored locally on this device.</p></div></header><Link className="page-back-link" href="/library">← Back to Library</Link><SavedList documents={getPublishedDocuments()} mode="recent"/></div>}
