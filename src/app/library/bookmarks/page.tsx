import Link from "next/link";import{getPublishedDocuments}from"@/features/library/data/repository";import{SavedList}from"@/features/library/ui/saved-list";export const metadata={title:"Bookmarks"};export default function Page(){return<div className="page"><header className="page-header"><div><span className="eyebrow">SAVED FOR LATER</span><h1>Bookmarks</h1><p>Your bookmarked procedures, stored locally on this device.</p></div></header><Link className="page-back-link" href="/library">← Back to Library</Link><SavedList documents={getPublishedDocuments()} mode="bookmarks"/></div>}
